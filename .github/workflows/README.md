# 🚀 CI/CD Pipeline - Документация

## 📖 Что такое CI/CD?

**CI/CD** (Continuous Integration / Continuous Deployment) - это автоматизация процессов разработки:

- **CI (Continuous Integration)** - автоматическая проверка кода при каждом изменении
- **CD (Continuous Deployment)** - автоматический деплой приложения

## 🎯 Что делает наш CI/CD Pipeline?

Наш pipeline выполняет следующие задачи:

### 1. ✅ Тестирование Backend
- Устанавливает зависимости
- Проверяет, что код компилируется
- (Опционально) Запускает линтер

### 2. ✅ Тестирование Frontend
- Устанавливает зависимости
- Запускает линтер (ESLint)
- Проверяет, что проект собирается (build)

### 3. 🐳 Сборка Docker образов
- Собирает Docker образ для Backend
- Собирает Docker образ для Frontend
- Использует кеш для ускорения сборки

## 📁 Структура файлов

```
.github/
└── workflows/
    └── ci-cd.yml          # Основной файл CI/CD pipeline
```

## 🔄 Когда запускается Pipeline?

Pipeline автоматически запускается в следующих случаях:

1. **Push в ветки**: `main`, `master`, `develop`
2. **Pull Request** в ветки: `main`, `master`, `develop`

## 🚦 Статусы выполнения

После каждого push или PR вы увидите статус выполнения:

- ✅ **Зелёная галочка** - все проверки прошли успешно
- ❌ **Красный крестик** - что-то пошло не так
- 🟡 **Жёлтый кружок** - проверка выполняется

## 📊 Как посмотреть результаты?

1. Перейдите в репозиторий на GitHub
2. Нажмите на вкладку **"Actions"**
3. Выберите нужный workflow run
4. Посмотрите детали выполнения каждого шага

## 🛠️ Локальное тестирование

Перед тем как пушить код, убедитесь, что всё работает локально:

### Backend:
```bash
cd backend
npm install
npm run dev  # или node index.js
```

### Frontend:
```bash
cd frontend
npm install
npm run lint
npm run build
```

### Docker:
```bash
# Проверка сборки образов
docker-compose build

# Проверка запуска
docker-compose up
```

## 🔧 Настройка Pipeline

### Добавление новых тестов

Чтобы добавить тесты в pipeline, откройте `.github/workflows/ci-cd.yml` и добавьте шаг:

```yaml
- name: 🧪 Запустить тесты
  working-directory: ./backend
  run: npm test
```

### Изменение условий запуска

Чтобы изменить, когда запускается pipeline, отредактируйте секцию `on:`:

```yaml
on:
  push:
    branches:
      - main
      - your-branch  # Добавьте свою ветку
```

## 🚀 Следующие шаги (опционально)

### 1. Публикация Docker образов

Чтобы автоматически публиковать образы в Docker Hub или GitHub Container Registry:

```yaml
- name: 🐳 Публиковать образ
  uses: docker/build-push-action@v5
  with:
    context: ./backend
    push: true  # Включить публикацию
    tags: your-username/dril-pipe-backend:latest
```

### 2. Автоматический деплой

Можно добавить автоматический деплой на сервер после успешной сборки.

### 3. Уведомления

Можно настроить уведомления в Slack, Telegram или email при успехе/ошибке.

## ❓ Частые проблемы

### Pipeline не запускается
- Убедитесь, что файл находится в `.github/workflows/`
- Проверьте синтаксис YAML (можно использовать онлайн валидатор)
- Убедитесь, что вы пушите в правильную ветку

### Ошибка при сборке Docker
- Проверьте, что Dockerfile существует и корректен
- Убедитесь, что все необходимые файлы присутствуют
- Проверьте логи в GitHub Actions

### Тесты не проходят
- Запустите тесты локально
- Проверьте, что все зависимости установлены
- Убедитесь, что переменные окружения настроены правильно

## 📚 Полезные ссылки

- [GitHub Actions Documentation](https://docs.github.com/en/actions)
- [Docker Documentation](https://docs.docker.com/)
- [YAML Syntax](https://yaml.org/spec/1.2.2/)

---

**Удачи в разработке! 🎉**

